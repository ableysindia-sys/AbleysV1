<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the one directory share cards are written to. Nothing else in the app's storage is
         exposed, and memory photos live in filesDir which is deliberately not listed here. -->
    <cache-path name="share" path="share/" />
</paths>
